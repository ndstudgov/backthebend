---
title: Back the Bend
active_page: signup
stylesheet: [project-cards]
page_name: signup
---

<div class="container-fluid projects-container">
  <% data.projects.each do |project| %>
    <%= partial("card", locals: {
      project_name: project.name,
      project_desc: project.description
    }) %>
  <% end %>
</div>
